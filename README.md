# Date

1. Frontend (UI)
- React (modern standard)
- Runs on: http://localhost:5173

2. Backend (API)
- Python Flask (clean API only)
- Runs on: http://localhost:5000

apt install python
create virtual environment
pip install flask flask-cors
apt install npm
npm create vite@latest frontend

Front-End
Framework: React
Variant: JavaScript

### GET vs POST
We're gonna use POST request body vs GET query parameters. Post is a payload or a package that contains the data you are posting. Get uses query so on the link you will see the parameters so its like someone telling you something verbally vs writing you a letter. the get method is in the old folder. 

TLDR
Before: Frontend sends data in URL
Now: Frontend sends structured JSON object

### Continuing

open two terminals.
python app.py

npm run dev

should open
Local: http://localhost:5173/